<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pmdm.ejemplo_adaptadores"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="pmdm.ejemplo_adaptadores.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ListaActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ListaModActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".GaleriaActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SpinnerActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ListaExpansibleActivity" >
        </activity>
    </application>
</manifest>
